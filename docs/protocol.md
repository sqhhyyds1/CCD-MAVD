# Experiment Protocol Notes

Use public pre-extracted features for the first implementation. Treat pseudo or latent context labels as generated regularization signals, not ground-truth scene annotations. Use `causal-inspired context debiasing` in writing unless a stronger causal identification argument is later established.
